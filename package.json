{
  "name": "networth",
  "private": true,
  "description": "Dashboard patrimonial — pipeline data → engine → render, statique (GitHub Pages).",
  "scripts": {
    "test": "node scripts/run_tests.mjs",
    "lint": "npx --no-install eslint js scripts tests eslint.config.mjs generate_snapshot.mjs",
    "desync": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/detect_desyncs.mjs",
    "confidentiality": "node scripts/verify_no_leak.mjs",
    "confidentiality:prod": "node scripts/verify_no_leak.mjs --prod",
    "verify": "npm run lint && npm run test && npm run desync",
    "encrypt": "node scripts/enable_encryption.mjs",
    "bump": "node scripts/bump_version.mjs"
  },
  "devDependencies": {
    "eslint": "^10.10.0"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.100.10"
  }
}
